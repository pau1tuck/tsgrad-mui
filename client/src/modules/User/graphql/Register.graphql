mutation Register(
  $firstName: String!
  $lastName: String!
  $email: String!
  $password: String!
) {
  register(
    firstName: $firstName
    lastName: $lastName
    email: $email
    username: $email
    password1: $password
    password2: $password
  ) {
    success
    errors
    token
  }
}
